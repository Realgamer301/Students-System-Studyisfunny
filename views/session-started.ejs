<%- include('partials/header') %>
  <div class="container py-5 text-center">
    <div class="card shadow-sm mx-auto p-4" style="max-width: 450px;">
      <h3 class="text-success mb-3">✅ الحصة بدأت</h3>
      <p>السنتر: <strong><%= session.Center.name %></strong></p>
      <p>المادة: <strong><%= session.Subject.name %></strong></p>
      <p>رقم الحصة النسبي: <strong><%= session.lesson_number %></strong></p>
      <p>السيريال النهائي: <strong class="text-primary fs-4"><%= session.serial_number %></strong></p>

      <a href="/sessions/new" class="btn btn-outline-secondary w-100 mt-3">بدء حصة تانية</a>
    </div>
  </div>
<%- include('partials/footer') %>